enabled: true
order: 50
segments: [src]
time_dim: loss_yr            # derived: year(evt_dt)
sot_query: sql/sot_loss.sql
sot_loss_col: allocation
sot_claim_count_col: claim_ant
dim_map: {}
tolerance_loss: {abs: 5000, pct: 0.02}
tolerance_claims: {abs: 5, pct: 0.02}
analytics:
  segments: [src]
  lr_basis: pol_yr           # written premium only for now -> policy-year basis
  frequency_per: 1000
  lr_cap: 5.0
  lr_bins: 50
